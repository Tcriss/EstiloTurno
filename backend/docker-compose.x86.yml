version: '3.8'
services:
  postgres:
    image: postgres:16-alpine
    container_name: estiloturno_db_x86
    platform: linux/amd64
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: estiloturno_secure_pass_2026
      POSTGRES_DB: estiloturno
    ports:
      - "5432:5432"
    volumes:
      - pgdata_x86:/var/lib/postgresql/data

volumes:
  pgdata_x86:
